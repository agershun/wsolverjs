if (typeof exports === 'object') {
	var assert = require('assert');
	var {LpProblem,Matrix,Vector,almost,LP_METHOD_BRUTE,LP_METHOD_INTPOINT,LP_METHOD_SIMPLEX} = require('..');
}

describe('55. readMps() ', function() {
	it('55.1.readMps() to the problem', async function(done) {
		// const p = await LpProblem.readMps('mps/prob1.mps');
		// TODO: Check read variables
		// p.solve();
		// assert(almost(p.optVal,99));
		done();
	});


});
